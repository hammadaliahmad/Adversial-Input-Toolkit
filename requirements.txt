# Adversarial Input Red-Teaming Toolkit — full dependency list
# Installs everything needed for BOTH toolkit.py (the toolkit) and
# app.py (the example FastAPI model under test).
#
# Note on torch: the line below pulls the default (CUDA-enabled) build,
# which is a multi-GB download. If installing on a machine with no NVIDIA
# GPU, install the CPU-only wheel first, then install the rest of this file:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

requests
pandas
streamlit
plotly
fastapi
uvicorn[standard]
transformers
torch
